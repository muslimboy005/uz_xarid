class AppConfig {
  /// The package name to use for loading assets.
  /// Should be null when running as a standalone app,
  /// and 'uz_xarid' when running as a package dependency.
  static String? packageName;
}
